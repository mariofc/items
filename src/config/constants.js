const GET = 'get';

const STATUS_CODE = {
  OK: 200,
};

const AUTHOR = {
  NAME: 'Mario',
  LAST_NAME: 'Fernández',
};

const ITEM_CONDITION = 'ITEM_CONDITION';
const NO_DISPONIBLE = 'No Disponible';
const TOTAL_REQUEST_RETRY = 3;

module.exports = { GET, TOTAL_REQUEST_RETRY, STATUS_CODE, AUTHOR, ITEM_CONDITION, NO_DISPONIBLE };
